{
  "name": "chowbenin",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter @chowbenin/web dev --port 5173 --host",
    "dev:dashboard": "pnpm --filter @chowbenin/dashboard dev --port 5174 --host",
    "build:web": "pnpm --filter @chowbenin/web build",
    "build:dashboard": "pnpm --filter @chowbenin/dashboard build",
    "check": "pnpm -r check",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
		"generate-database-types": "supabase gen types --local > packages/shared/src/types/database.types.ts"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
